# generated by tools/protocolgen — edit the declarations in sieve/protocol, not this file
openapi: 3.1.0
info:
  title: Sieve HTTP contract
  version: 1.0.0
  description: |-
    The typed JSON endpoints, generated by tools/protocolgen from sieve/protocol.
    Descriptions are the godoc comments and doc: struct tags on the declarations themselves.
    Routes serving hypermedia or bytes are deliberately absent: their contract is the
    template they render, and docs/API.md inventories them.
paths:
  /api/thing:
    post:
      operationId: postApiThing
      summary: FixtureCreateRequest makes a fixture thing.
      description: |-
        FixtureCreateRequest makes a fixture thing. Every field travels in the body,
        so this type's own component IS the request body the operation references —
        which FixturePatchRequest, split across the query string, deliberately is not.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenFixtureCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GenFixtureCreateRequest'
      responses:
        "200":
          description: fragment
          content:
            text/html:
              schema:
                type: string
  /api/thing/{id}:
    patch:
      operationId: patchApiThingId
      summary: FixturePatchRequest changes a fixture thing's name.
      description: FixturePatchRequest changes a fixture thing's name.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: owner
          in: query
          required: true
          description: who is asking; a query parameter, not a body field
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: the new name
              required:
                - name
      responses:
        "200":
          description: json
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenFixturePatchResponse'
components:
  schemas:
    GenFixtureCreateRequest:
      type: object
      description: |-
        FixtureCreateRequest makes a fixture thing. Every field travels in the body,
        so this type's own component IS the request body the operation references —
        which FixturePatchRequest, split across the query string, deliberately is not.
      properties:
        name:
          type: string
          description: what to call it
      required:
        - name
    GenFixturePatchResponse:
      type: object
      description: FixturePatchResponse reports the version the patch produced.
      properties:
        version:
          type: integer
      required:
        - version
