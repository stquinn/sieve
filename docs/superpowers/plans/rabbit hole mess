Continue implementing the Block Document Model on branch feature/refactor_editor_layout.

  Plan: docs/superpowers/plans/2026-06-17-block-document-model.md — read the Progress/handoff log and File
  map first to load the whole arc.

  Where the arc stands:
  - Stage A (backend BlockDoc + serialization spine) — DONE
  - Stage B (universal handles + bijection) — DONE
  - Stage C (block-ops wire protocol over WS) — Go side DONE
  (block_op.go/HandleBlockOp/ws_handler/computeBlockSync)
  - Stage D (native frontend) — IN PROGRESS ← we are here
  - Stage E (containers/tree + columns; retire blockRef) — ahead
  - Stage F (lenses + server-side tree search) — ahead

  Current chunk = Stage D, node-granular prose. Design locked in
  docs/superpowers/specs/2026-06-19-node-granular-prose-blocks-design.md and memory note
  project_node_granular_prose. The settled model (do not re-litigate): a kind:prose block = one top-level
  native TipTap node (paragraph/heading/list/table/blockquote), one construct per block; no custom prose
  node, no block+ wrapper, no keymap; identity = a blockId global attr, durable identity in the paired
  <!--s:ID-->…<!--/s:ID--> markers; observer emits per-node {uuid, blockId, content} ops.

  Resume at plan task D-r.2+D-r.3 (entangled, land together): addGlobalAttributes(blockId) on native
  top-level types; generalize per-node marker serialization; render one node per block on load; retire the
  custom sieve-prose container. Then D-r.4 (mint — watch ProseMirror's splitBlock attr-copy duplicate-id
  trap), D-r.5 (retire prose doc-update fallback), D-r.6 (WebKit regression).

  Discipline: follow the plan's execution sub-skill (subagent-driven-development or executing-plans), TDD,
  keep the app runnable each step, and gate every UI task in the real WebKitGTK app (mounts, zero console
  errors, typing emits update-block without changing block count, Enter creates exactly one new delimited
  block).

  Start by reconciling current code against the plan (some Stage D D.1–D.3 code predates the 2026-06-19
  pivot), then bite-size and execute D-r.2+D-r.3. When Stage D lands, continue to Stage E.



  -----------------------------------------


So you re losing the run of this completely.

  And we have massively overengineered this.  PM HAS NODES.  IT MANAGES NODES.

  SIEVE HAS BLOKCS IN THE BACKEND.  IN a very crude manner all we are doing is rendering SIEVE BLOCKS OF
  VARIOUS TYPES as PM Nodes.  Eitehr standard p[rose nodes or our fancy rich Sieve Blocks (codes,
  diagrams, ai etc)
  
  So SIeve Blocks have afvarious attributes.  Code Block have
  a source.  Ai have repsonses.  Prose have CONTENT.  Content is JUST a string.  Thats it.

  When rendering that CONTENT string we need to just hand it to prose mirror as its content dom.  Thats
  it.  When the content DOM changes - save it aghin as the content of the Prose block
  ⎿  Interrupted · What should Claude do instead?

❯ So if thats complex MARKDOWN and pm creates MANY sub NODES - thats PM business.



  Every Sieve block is { type, + type-specific attributes }:
  - code → source
  - ai → response
  - prose → content — and content is just a string.

  Rendering a prose block: hand its content string to ProseMirror as the node's contentDOM. PM parses that
  markdown and makes whatever sub-nodes it wants — that's PM's business, we don't look inside.

  Saving: when the contentDOM changes, serialize it back to a string and store it as that prose block's
  content. Done.

  So prose isn't special. It's just another block whose payload happens to be markdown handed to a
  contentDOM. No split keymaps, no flat-node schema rebuild, no chrome investigation — I was inventing all
  of that because I'd convinced myself "prose block = a special structural thing." It's a block with a
  string.
