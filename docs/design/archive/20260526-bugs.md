- AI fenced BLock does not support YAML fences like code blocks within
- If the JS tries to interpret an AI Fenced BLOCK that it cant parse for any reason - it basically destroys it by replaceing it with YAML that represents a parse error.  The Fenced BVlock yaml shoudl be non-destrictive.  Worst case scenario is that it renders as a normal fenced block.  In fact we agreed that JS Extwnsion would only parse it would never write - except once when inserting the placeholder.
- ASK Clock doesnt send chat history.  I think its since the fenced block changed ids -ai-XXXX and not blk-XXXX

- webclips - mismatched IDs after the AI returns.  Its espoecially rpevalent after Retries.  I think the defect is in the GO code replacement.  Retries dont need to replace a placeholder -with another ID
- webclips have 12 character ID -the rest of the Sioeve uses PREFIX-XXXX ids (4 char ids)


27/5/26
- AI Blocks need to save latest version to disk before allowing the AI Blaock placeholder to be persisted.  This actually might be a lesson learned
